<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            text-align: center;
        }

        label {
            padding: 3px;
        }

        body {
            background-color: bisque;
        }

        form {
            width: 40%;
            margin: auto;
        }

        a {
            display: block;
        }
    </style>
</head>
<body>
    <form action="/check" method = "post">
        <div><label for="">Username</label><input name="username" type="text"></div>
        <div><label for="">Password</label><input name="password" type="text"></div>
        <button type = "submit">Submit</button>
        <a href="/create">Create Account</a>
        <% if (buildIt) { %>
            <% if (build) { %>
                <p>Incorrect Login Information</p>
            <% } else { %>
                <p>Logged in</p>
            <% } %>
        <% } %>
    </form>
</body>
</html>